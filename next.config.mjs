/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        remotePatterns:[
            {hostname:"dependable-ferret-649.convex.cloud"}
        ]
    }
};

export default nextConfig;
